function solveKepler(e, M){
  let E = M;
  
  while(abs(f(E, M, e)) > 0.00000001){
    E = E - (E - e * sin(E) -M)/(1 - e * cos(E));
  }

  return E;
}

function f(E, M, e){
  return E - e * sin(E) - M;
}
